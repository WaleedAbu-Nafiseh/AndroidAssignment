<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SelectedItemDetailsActivity"></activity>
        <activity android:name=".view.SelectedItemsActivity" />
        <activity android:name=".view.Food.FoodListActivity" />
        <activity android:name=".view.Food.FoodDetailsActivity" />
        <activity android:name=".view.Electronics.ElectronicsDetailsActivity" />
        <activity android:name=".view.Electronics.ElectronicsListActivity" />
        <activity android:name=".view.cloths.ClothesDetailsActivity" />
        <activity android:name=".view.cloths.ClothesListActivity" />
        <activity android:name=".view.cars.CarDetailsActivity" />
        <activity android:name=".view.cars.CarListActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>